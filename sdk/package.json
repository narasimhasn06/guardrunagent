{
  "name": "@guardrunagent/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "GuardrunAgent SDK -- Claude Code hooks for event logging and guardrail checks",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
