export default function HomePage() {
  return (
    <div>
      <h1 className="page-title">Home</h1>
      <p className="page-placeholder">
        Org overview (stat cards, spend chart, recent activity) is built in a later step -- this
        confirms the authenticated shell and navigation work.
      </p>
    </div>
  );
}
