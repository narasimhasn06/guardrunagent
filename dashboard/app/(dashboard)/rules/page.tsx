export default function RulesPage() {
  return (
    <div>
      <h1 className="page-title">Guardrail Rules</h1>
      <p className="page-placeholder">
        Rules tab and Activity Log (GET/POST /rules, POST /rules/starter) are built in a later
        step.
      </p>
    </div>
  );
}
