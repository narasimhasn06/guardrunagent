export default function SessionsPage() {
  return (
    <div>
      <h1 className="page-title">Sessions</h1>
      <p className="page-placeholder">
        Session list (GET /sessions, filters, search) is built in a later step.
      </p>
    </div>
  );
}
